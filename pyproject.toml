[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vishai-next"
version = "0.1.0"
description = "Production-grade capability-based AI Operating System."
authors = [
    { name="VishAI Team", email="vishalaianimation@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    # Core dependencies will be added here
]

[tool.setuptools.packages.find]
include = ["vishai*"]
